Para compilar esse projeto, deve-se reunir todos os arquivos em uma única pasta e utilizar o seguinte comando:
gcc -o trab3 *.c -lm
Para executar o arquivo binário, é utilizado o seguinte comando, passando como parâmetro o nome do arquivo com os comandos:
./trab3 arquivoSimular.txt
